#include <algorithm>
#include <cmath>
#include <cstring>
#include <deque>
#include <functional>
#include <iostream>
#include <queue>
#include <stack>
#include <utility>
#include <vector>
using namespace std;
int main()
{
    {
        cout << "trying out bitwise operators" << endl;
        int a = 4, b = 6;
        cout << " a&b " << (a & b) << endl;
        std::cout << "a|b" << (a | b) << std::endl;
        std::cout << "~a" << ~a << std::endl;
        std::cout << "a^b" << (a ^ b) << std::endl;

        return 0;
    }
}
